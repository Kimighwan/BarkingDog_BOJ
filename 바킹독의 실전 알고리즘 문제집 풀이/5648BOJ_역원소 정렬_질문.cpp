#include <iostream>
#include <vector>
using namespace std;

int n;
string str;
vector<long long> v;

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> str
	}
}